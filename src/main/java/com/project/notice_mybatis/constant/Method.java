package com.project.notice_mybatis.constant;

//클래스처럼 보이게하는 상수.
//Enum은 상수를 처리하는 목적으로 사용되는 클래스.
//열거된 순서에 따라 0부터 순서값을 가진다.
// 열거형 변수는 선언후 ; 은 붙이지 않는다.
// 상수와 특정값을 연결시킬경우 마지막에 ;를 붙인다.
public enum Method {
    GET,POST,PUT,PATCH,DELETE
}
